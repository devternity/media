mjml
  mj-body
    mj-container(background-color="#E1E8ED")
      mj-section(padding="20px" background-color="white")
        mj-image(href="https://devternity.com" src="https://devternity.com/images/dt_logo_rect.png" width="50px")
      mj-section(padding="20px" background-color="white")
        mj-column(width="100%")
          mj-text(align="center")
            h2 Hey there! How's it going? 👋
          mj-text(align="center" color="grey") We are happy to announce that registration to 
            b: a(href="https://devternity.com") #{theme}
            |  is open.
          mj-text(align="center" color="grey") This year you can enjoy #{talks_total} premium-quality talks and choose among #{workshops_total} hands-on power workshops.
          mj-text(align="center" color="grey") The topics include 
            b Clean Architecture, Coding, Testing, Refactoring, Jedi Techniques, Career, 
            |  and a lot more.
          mj-text(align="center" color="grey") 
            | Ranked among 
            b  the top 3 tech conferences in Europe,
            |  DevTernity has become the most waited event for top developers, architect and engineering leaders. Join the movement!

      mj-section(background-color="#f3f3f3" padding-top="20px")
        mj-text(align="center" color="#a5b0b8")
          h2 Some of our speakers

      each p in program
        if p.event == "keynotes"
          each speech in p.schedule
            if speech.pitch
              mj-section(background-color="#f3f3f3" padding-top="20px")
                mj-column(width="130")
                  mj-image(src=`https://devternity.com/${speech.img}` width="100" border-radius="100px")
                mj-column(width="350")
                  mj-text(align="center" font-size="16" color="grey") #{speech.name} (#{speech.country_long})
                  mj-text(align="center" color="grey") #{speech.pitch}

      mj-section(background-color="#ffffff" padding="20px")
        mj-text(align="center" color="#a5b0b8")
          h2 What's next?
          mj-text(align="center") We brought the best speakers to Riga, so you can learn directly from industry influencers. How it's your turn.
      mj-section(background-color="#ffffff")
        mj-column(width="100%")
          mj-button(href=`${selfLink}` background-color="#3ba666" color="white") Count me in

      mj-section(background-color="#ffffff" padding-top="20px")
        mj-column(width="100%")
          mj-divider(border-width="1px" border-style="dashed" border-color="lightgrey")
          mj-text(align="center" color="grey" font-size="11")
            | Bored developer, not attending conferences? 
            a(href="[Unsubscribe]") we got you covered